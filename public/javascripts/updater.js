define(function() {
  var Updater = Class.extend({
    init: function(game) {
      this.game = game;
    },

    update: function() {
      // console.log('update!');
    }
  });

  return Updater;
});